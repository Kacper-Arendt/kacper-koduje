import styled from "styled-components";

const Div = styled.div`
  grid-column: start / end;
`

export const AboutMe = () => {
    return (
        <Div id='aboutMe'>
            About Me
        </Div>
    )
}